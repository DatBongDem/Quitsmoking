name: Build Java Web Project (Ant)

on:
  push:
    branches: [ "master" ]  
  pull_request:
    branches: [ "master" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v3

    - name: ☕ Setup Java
      uses: actions/setup-java@v3
      with:
        java-version: '8'
        distribution: 'temurin'

    - name: 🛠 Install Ant
      run: sudo apt-get install ant -y

    - name: 🧱 Build with Ant
      run: ant -buildfile build.xml
